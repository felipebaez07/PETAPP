{
  "name": "petapp",
  "private": true,
  "version": "0.1.0",
  "description": "Plataforma Integral de Bienestar Animal — Fase 1 · Piloto Ibagué (nombre provisional: PetApp)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile",
    "typecheck": "npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/mobile && npm run typecheck --workspace=packages/shared",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
